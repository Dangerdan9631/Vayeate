# Vayeate Theme Studio

Repository root for the Vayeate Theme Studio Electron application.

## Placement

- Path: `.`
- Layer: Repository

## Contents

- Subfolders: `.agents`, `.specify`, `data`, `electron`, `patches`, `previews`, `public`, `scripts`, `specs`, `src`, `test`
- Files: `.dependency-cruiser.cjs`, `.editorconfig`, `.gitignore`, `AGENTS.md`, `architecture-graph.html`, `dependency-graph.html`, `eslint.config.js`, `index.html`, `package-lock.json`, `package.json`, `tsconfig.json`, `vite.config.ts`, `vite.electron-dev-shutdown.plugin.ts`, `vite.renderer-plugins.ts`, `vitest.config.ts`

## Rules

- Follow AGENTS.md for layer boundaries, naming, dependency direction, and mutation flow.

See the repository root AGENTS.md for the complete architecture and mutation-flow rules.

