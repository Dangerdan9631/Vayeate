---
description: Rules for authoring and modifing react components App state
alwaysApply: false
---

# State

## Shape

- **One** root aggregate; **only nested slice objects** — no loose fields on root.
- **Immutable** updates (replace slice references).

## Access

| Consumer | Read | Write |
|----------|------|-------|
| Controllers / operations | **Getter** | **Setter** (operations **only**) |
| `ActionQueue` (`src/app/core/actions/`) | Via existing accessors if needed | **Queue status only** — dedicated setter invoked **only** from `ActionQueue` (see [app-architecture.mdc](app-architecture.mdc)) |
| React | Via **viewmodel** + `useContextSelector` | Never |

## Rules

- Slices are **domain-owned** under `src/domain/<domain>/state/`.
- React **never** imports setters directly.
- **Do not** call the queue-status setter from components, handlers, controllers, or operations — **only** from `ActionQueue`.

## Good / bad

```ts
// BAD — component imports setter
import { setCatalog } from '../state';

// GOOD — operation
this.state.setCatalog(nextImmutableSlice);
```
